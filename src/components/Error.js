const Error = () => {
  return (
    //Just a big red error image to let the user know something went wrong. Too lazy to type out the error message or use more files for each error sorry =(
    <img
      src="https://www.freeiconspng.com/thumbs/error/red-circular-image-error-0.png"
      id="ErrorImg"
      className="img-fluid"
      alt="Error"
    />
  );
};

export default Error;